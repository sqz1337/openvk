SELECT
  *
FROM
  `messages`
WHERE
  (`deleted` = 0)
  AND (`conversation_id` IS NULL)
  AND (`id` > ?)
  AND (
    (
      (`sender_type` = ?)
      AND (`recipient_type` = ?)
      AND (`sender_id` = ?)
      AND (`recipient_id` = ?)
    )
    OR (
      (`sender_type` = ?)
      AND (`recipient_type` = ?)
      AND (`sender_id` = ?)
      AND (`recipient_id` = ?)
    )
  )
ORDER BY
  `created` DESC
LIMIT
?
OFFSET
?
